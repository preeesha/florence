/**
 * Dependency list (useEffect, functional components):
 *      1. Nothing's provided: Callback will run on every event (mount, unmount, update of any value)
 *      2. []: Callback will run only when the component is mounted.
 *      3. [name, users]: Callback will run only when the value of state variables "name" & "users"
 */

/**
 * 
 */
